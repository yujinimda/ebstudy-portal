# 명세 품질 체크리스트: 003 공지사항

**목적**: 계획 단계로 넘어가기 전 명세의 완결성과 품질을 검증한다
**작성일**: 2026-07-31
**대상**: [spec.md](../spec.md) — **AC 43개 · FR 29개**
**반복 회차**: **1회** (4번 계획을 쓰면서 동시에 검사. `/speckit-clarify` 미실행)

**표기**: `[x]` 충족 · `[ ]` **미충족** · `[~]` **의도적 예외**(근거를 함께 기록)

**결과: 항목 21개 — 15 충족 / 3 미충족 / 3 의도적 예외**
*(001은 항목 16개였고, 003은 "상속 스토리의 체크" 5개를 신설해 21개다)*

> ⚠️ **이 문서는 "통과했다"고 말하지 않는다.**
> 003은 `002-boards/requirements.md` 머리말이 밝힌 대로 **정식 스펙 절차의 3번 게이트
> (`/speckit-clarify` + 사람 확정)를 건너뛴 상태**다. 미충족 3건은 **그 사실의 결과**이며
> 숨기지 않는다 — `process.md` 5장의 3번 게이트 종료 조건은 *"미확정 항목 0개"* 인데
> **003은 판단 항목 17건 + 계획이 올린 미해결 9건을 안고 있다.**

---

## Content Quality

- [~] **구현 세부가 없다 (언어·프레임워크·API)** — **의도적 예외 1건, 근거 기록됨**
  - AC에 **HTTP 상태코드와 오류 코드**를 포함했다. constitution 원칙 II(NON-NEGOTIABLE)가
    검증 가능한 AC를 요구하고 이 AC들이 그대로 테스트·QA 체크리스트가 되기 때문이다.
    상태코드·오류 코드는 프레임워크가 아니라 **API 계약**이다. 001과 같은 판단.
  - **프레임워크·라이브러리·DB 종류는 등장하지 않음을 확인했다.** 001에서 예외였던
    *"`FR-008`에 알고리즘 명시"* 에 해당하는 항목이 003에는 없다.
- [x] 사용자 가치와 비즈니스 요구에 집중 — 각 FR에 *왜 그렇게 정했는지*가 한 줄씩 붙어 있다
- [~] **비기술 이해관계자가 읽을 수 있다** — 상태코드 표기 때문에 완전한 비기술 문서는 아니다.
      위 예외와 같은 근거. **개요의 질문 6개 표**와 각 AC의 *근거* 줄은 평서문으로 유지했다
- [x] 필수 섹션 모두 작성 (User Scenarios / Requirements / Success Criteria)

## Requirement Completeness

- [ ] **미확정 항목이 없다** — **미충족.** 003의 가장 큰 결함이며 계획으로 해소되지 않았다
  - **판단 항목 17건**(J1~J17)이 *(Claude 판단 — 사람 검증 필요)* 상태로 남아 있다.
    그중 **J4**(최신 5개의 기준) · **J5**(검색 시 고정 처리) · **J6**(고정 행의 분류명) ·
    **J10**(관리자 전권) · **J13**(관리자 로그인 진입점)이 **뒤집히면 AC를 재작성해야 하는**
    항목이다
  - **4번 계획이 미해결 9건을 추가로 올렸다** — 그중 **1·2·3·7·8은 002·004가 확정할 것**이고
    **4·5·6·9는 사람 판단**이다
  - **`[NEEDS CLARIFICATION]` 마커 자체는 0건**이다. 다만 그것은 *"질문을 마커로 남기는 대신
    판단하고 표시했다"* 는 뜻이지 **해소됐다는 뜻이 아니다** — 마커 개수로 이 항목을
    충족 처리하면 **게이트가 형식만 남는다**
- [x] 요구사항이 검증 가능하고 모호하지 않다 — AC 43개 전부 `Given/When/Then + AC-ID`,
      **Given이 하나도 빠지지 않았다.** 상태코드와 오류 코드까지 명시
- [x] 성공 기준이 측정 가능하다 — SC-001~009. *"0건"* · *"100%"* 처럼 세어지는 형태다
- [x] 성공 기준이 기술 중립적이다 — SC에 기술 용어 없음
- [x] 모든 인수 시나리오가 정의되어 있다 — AC-1 ~ AC-43
- [x] 엣지 케이스가 식별되어 있다 — **3분류 18항목**
      (상단 고정·페이징 경계 8 / 입력 경계 6 / 권한·상태 경계 4)
- [x] 범위가 명확히 경계 지어져 있다 — Out of Scope 7항목.
      **메인 페이지의 공지 영역을 명시적으로 제외**하고 *"메인에는 페이징이 없어 `FR-004`가
      성립하지 않는다"* 는 이유까지 적었다
- [x] 의존 관계와 가정이 식별되어 있다 — Assumptions 10건 · 의존 3표
      (001에서 끝난 것 6 / 다른 스토리가 정할 것 5 / 003이 넘기는 것 0)

## Feature Readiness

- [x] **모든 기능 요구사항에 대응하는 인수기준이 있다** — **1회차에서 미충족이었고 수정함**
  - **발견**: 계약을 쓰다 **엔드포인트가 반환해야 하는데 AC가 없는 응답 2개**가 나왔다 —
    ① 다른 게시판의 분류·존재하지 않는 분류로 등록하는 경우
    ② **삭제된 공지에 대한 수정·삭제 요청**(Edge Cases에 규칙만 있고 AC가 없었다)
  - **조치**: **`FR-029` · `AC-42` · `AC-43` 신설.** FR → AC 매핑 표와 역방향을 다시 세어
    **AC 없는 FR 0건 · 어느 FR에도 속하지 않는 AC 0건**을 확인했다
  - **찾은 기준**: *"AC가 없으면 커버리지 분모에 들어가지 않으므로 아무도 세지 않는다"*
    (001의 5번 게이트가 `AC-35`~`AC-37`을 찾은 것과 같은 기준)
- [x] 사용자 시나리오가 주요 흐름을 덮는다 — US1 목록 / US2 상단 고정 / US3 상세 /
      US4 등록·수정·삭제 / US5 권한 경계. **US2가 003이 새로 만드는 유일한 것**이고
      AC 13개(`AC-6`~`AC-18`)로 가장 두껍다
- [x] 성공 기준의 측정 가능한 결과를 만족한다 — SC-003(같은 글 두 번 0건) ·
      SC-004(고정 후 사라지는 글 0건) · SC-005(번호 연속)가 각각 `AC-8` · `AC-10` · `AC-13`에
      대응한다
- [~] 구현 세부가 명세로 새지 않는다 — 위 Content Quality의 의도적 예외와 동일

## 003 고유 항목 — 상속 스토리의 체크

001에는 없던 항목이다. **003은 공통 구조의 소비자**라 여기서 결함이 난다.

- [x] **상속하는 것을 다시 정의하지 않았다** — *"002 에서 상속하는 것"* 표 9행이 참조만 하고
      값을 복제하지 않는다. 같은 규칙을 네 번 쓰면 **네 벌이 조금씩 어긋나고 문서로는
      발견되지 않는다**
- [x] **상속이 실제인지 확인하는 AC가 있다** — `AC-1`·`AC-3`·`AC-4` 3개.
      *"참조만 했다"* 로 끝내면 003에서 상한이 조용히 풀려 있어도 아무도 모른다
- [ ] **상속 대상이 확정되어 있다** — **미충족.** 002가 아직 확정되지 않아
      `FR-001`·`FR-019`가 가리키는 대상이 비어 있고, `AC-4`가 가리킬 오류 코드가 없다.
      **003의 결함이 아니라 순서의 결과**이며 `plan.md` 미해결 1·2로 추적한다
- [ ] **참조하는 리소스가 존재한다** — **미충족.** 분류(카테고리)는 **004 소유**이며
      아직 없다. `AC-29`·`AC-30`·`AC-32`·`AC-42`를 검증할 데이터가 없다.
      → **스텁을 만들지 않는다**(`test-strategy.md` 5.3 — *분모를 채우는 자기 기만*).
      **AC 본문을 그대로 두고 004의 분류 테이블이 생긴 뒤 검증한다.**
      **다른 스토리로 이관하지는 않는다** — 검증 자리는 003이 맞고 **시점만 뒤**다
- [x] **003이 다른 스토리에 넘기는 것이 없음을 확인했다** — 상단 고정은 공지 전용이고
      003은 새 공통 요소를 내놓지 않는다. 넘기는 것이 있는데 적지 않으면
      **받는 쪽이 그것을 처음부터 다시 발견해야 한다**

## 매핑 확인 (FR → AC)

| FR 범위 | 대응 AC |
|---|---|
| FR-001 ~ FR-003 (목록 공통 상속) | AC-1 · AC-2 · AC-3 · AC-4 · AC-5 · AC-20 |
| **FR-004 ~ FR-013 (상단 고정)** | **AC-6 ~ AC-18** |
| FR-014 ~ FR-018 (상세) | AC-19 · AC-21 · AC-22 · AC-23 · AC-24 · AC-25 · **AC-43** |
| FR-019 ~ FR-025 (등록·수정·삭제) | AC-18 · AC-26 ~ AC-32 · AC-34 · AC-35 · AC-36 · AC-37 · **AC-43** |
| FR-026 ~ FR-028 (권한) | AC-33 · AC-38 · AC-39 · AC-40 · AC-41 |
| **FR-029 (분류 참조 무결성)** | **AC-42** |

**AC 없는 FR: 0개 · 어느 FR에도 속하지 않는 AC: 0개.** 상세 표는 `spec.md`의 FR → AC 매핑 표.

> 001 체크리스트의 판정 2건이 5번 게이트에서 **뒤집혔다**는 사실을 기억하고 이 표를 만들었다.
> 그때 뒤집힌 이유는 *"FR 범위를 뭉쳐 적어서 그 안의 한 요구(`FR-014`의 **개별** 무효화)가
> 덮이지 않은 것이 드러나지 않았다"* 였다.
> → **003은 뭉치지 않고 `spec.md`에 FR 29행을 한 줄씩 적었다.** 위 표는 그것의 요약일 뿐이며
> **판정의 근거가 아니다.**

---

## Notes

**다음 단계(6번 태스크 분해) 전 상태**: **조건부 진입 가능.**
미확정 항목이 0이 아니므로 정식 절차라면 진입할 수 없다. 002 requirements 머리말이
*"정식 스펙 절차를 건너뛰고 구현에 바로 들어간다. 대가는 `STATUS.md`에 적는다"* 로
그 대가를 이미 선언했으므로, **여기서는 대가의 내용을 구체적으로 적는 것**이 할 일이다.

**받아들이는 대가 3개 — 무엇이 뒤집히면 무엇을 다시 하나**

| | 뒤집히면 | 다시 해야 하는 것 |
|---|---|---|
| 1 | **J4**(최신 5개 = 등록일시) | **데이터 모델 변경**(고정 지정 시각 보관) + `FR-005`·`FR-012`·`AC-9` 재작성 + 인덱스 정의 변경. **6.7 이후면 파괴적 변경** |
| 2 | **J5**(검색 시 고정 숨김 + 제외 해제) | `FR-013` 삭제 · `AC-16` 재작성 · **목록 조회 흐름(계약 ★2)이 통째로 바뀐다** |
| 3 | **J10**(관리자 전권) | `FR-027`·`AC-33` 재작성 + 소유자 검증 추가 + **"관리자 인수인계"와 감사 기록 요구사항이 새로 필요해진다** |

**계획(4번)에서 확정하지 못하고 남긴 것** — `plan.md` *미해결 항목* 9건.
그중 **6.7(마이그레이션) 전에 반드시 사람이 정해야 하는 것 2개**:

1. **`ADR-016`** — 고정 순서의 기준(등록일시 vs 고정 지정 시각 vs 지정 노출 순서)
2. **`ADR-010`** — 삭제 방식(물리 vs 논리)

**둘 다 컬럼이 늘어나므로, 테이블이 만들어진 뒤에 정하면 파괴적 변경**이 된다
(constitution 원칙 VI의 expand-contract가 발동한다).

**태스크 분해(6번) 시 주의**

- **묶음 AC 3개를 참조마다 쪼갠다.** 하나로 뭉치면 어느 검증이 빠졌는지 드러나지 않는다.
  - `AC-31`(직접 호출 우회) → `AC-27`·`AC-28`·`AC-29`·`AC-30` **각각 4개**
  - `AC-39`(`USER` 거부) → **등록 · 수정 · 삭제 3개**
  - `AC-43`(삭제된 공지) → **수정 · 삭제 2개**
- **`AC-3`(검색조건 유지 E2E)을 깎을 수 있는지 여기서 판단한다.** 002가 공통 목록 훅에
  같은 E2E를 두면 003은 중복이다. 대체 레벨은 `plan.md` *E2E 예산* 절에 준비해 뒀다
- **US1과 US2를 분리하지 않는다.** 고정은 번호·정원·정렬·검색 전부와 맞물려 있어
  나중에 얹으면 목록 조회를 다시 설계하게 된다

**의도적 예외 3건 (변경 없음)**

Content Quality의 3개 `[~]` 항목 — AC의 HTTP 상태코드·오류 코드 표기와
그로 인한 비기술 가독성 손실. constitution 원칙 II가 직접 요구하는 사항이므로
일반 명세 지침보다 우선한다. 001과 같은 판단이며 근거는 각 항목에 기록됐다.

**미해결로 남은 사항**

- **판단 17건이 사람 검증을 받지 못했다.** 이것이 003 스펙의 상태를 결정하는 단 하나의 사실이다.
- **E2E 전역 예산의 배분 규칙이 없다**(`plan.md` 미해결 9). 003은 자기 몫을 5→2로 깎았으나
  **스토리마다 자기 개수만 세는 구조 자체는 그대로**다.
