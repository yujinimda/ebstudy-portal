# Phase 0 — 기술 결정 (001 인증)

> `/speckit-plan` Phase 0 산출물. **plan.md의 NEEDS CLARIFICATION을 0건으로 만드는 문서.**
> 스택 전역 결정(Java 25 · Spring Boot 4.1 · Gradle · PostgreSQL)은 `process.md` 6.1에 있고
> 여기는 **001 범위에서 새로 정한 것**만 담는다.

**작성일**: 2026-07-29 | **브랜치**: `001-user-auth`

---

## 1. ORM — Spring Data JPA, 보조 조회 도구는 미룬다

**결정**: 기본 **Spring Data JPA(Hibernate)**. **복잡한 조회 도구(QueryDSL·JOOQ·MyBatis)는 추가하지 않고
필요해지는 시점까지 미룬다.** → **`ADR-004`**

**결정적 근거 — 우리 규칙이 이미 JPA를 전제하고 있었다**

`constitution` 원칙 VI와 `process.md` 4.7이 **`ddl-auto=validate`를 규칙으로 박아뒀다**:

> `ddl-auto=validate`로 둔다. 테이블을 만들지는 않지만 **"엔티티와 실제 스키마가 어긋났다"를
> 앱 부팅 때 잡아준다.** 공짜 안전망.

**`ddl-auto`는 JPA/Hibernate 기능이다.** MyBatis에는 대응 기능이 없다.
→ MyBatis를 고르면 **원칙 VI를 개정하고 그 안전망을 포기**해야 한다.

이 안전망이 특히 중요한 이유: 6.7에서 **마이그레이션 SQL을 사람이 손으로 쓴다.**
손으로 쓴 스키마와 자바 엔티티가 어긋나는 것이 **실제로 일어날 종류의 실수**이고,
`validate`가 그것을 **부팅 시점에** 잡는다. 런타임에 쿼리가 터지는 것보다 훨씬 이르다.

**기각한 대안**

| | 기각 이유 |
|---|---|
| **MyBatis** | `ddl-auto` 대상 기능이 없어 **원칙 VI 개정 + 안전망 포기**가 필요하다. 논리가 약하지는 않았다 — *모든 SQL이 눈에 보인다*는 점은 `ADR-003`에서 대소문자 무시를 `LOWER()` 인덱스로 고른 이유(*"규칙이 스키마에 보이게"*)와 같은 취향의 연장이다. **그 취향을 이기는 것은 안전망의 구체성이었다** |
| Spring Data JDBC | 배울 것이 적고 SQL에 가깝지만 **`ddl-auto`가 없고** 실무 사례·자료가 적다. 막혔을 때 찾을 것이 적은 것은 학습 단계에서 실질 비용이다 |
| JPA + 보조 도구를 **지금** 함께 도입 | **001에 복잡한 조회가 없다.** 목록·검색·페이징이 없고 전부 단건 조회·삽입이다. 쓰지 않을 도구를 지금 넣으면 배울 것만 늘어난다 (YAGNI) |

**받아들인 대가 (명시)**

1. **학습 곡선이 크다** — 영속성 컨텍스트 · 지연 로딩 · N+1 문제. JPA를 "SQL을 안 써도 되는 도구"로
   이해하면 반드시 사고가 난다
2. **SQL이 자동 생성되어 눈에 안 보인다** — 학습 목표와 부딪히는 지점이다
   → 완화 두 가지: **① `show-sql`을 켜서 생성된 SQL을 항상 본다.
   ② 통합 테스트가 실제 DB에 실제 SQL을 날리므로**(`test-strategy.md` 1장)
   **생성된 SQL이 실물로 검증된다.** 목(mock)으로 발랐으면 이 완화가 불가능했다

**미룬 것의 방아쇠**: **002 자유게시판의 목록 + 검색 + 페이징**에서 판단한다.
그때 JPA만으로 불편한지가 실측된다. 지금 추측으로 도구를 고르지 않는다.

---

## 2. 프론트 구성 — Next.js 하나로 사용자·관리자 둘 다

**결정**: **Next.js 앱 하나.** 사용자 화면과 관리자 화면을 **경로로 분리**한다.
*(사용자 결정 — 프론트엔드가 본업이므로 판단 근거가 사용자에게 있다)*

```
frontend/
  app/
    (user)/                 사용자 영역
      login/                사용자 로그인          AC-1·2·3·29
      signup/               회원가입 + 중복확인     AC-8~20·30
      posts/new/            인증 필요 화면(복귀 검증용)  AC-24·34
    admin/                  관리자 영역 — 별도 경로  FR-032
      login/                관리자 로그인          AC-31·32·33
      (dashboard)/          관리자 화면            AC-22·23·26
```

**`FR-032`(진입점 분리)가 경로 구조로 드러난다.** 같은 앱이지만 `/login`과 `/admin/login`이
다른 화면이고, 관리자 영역은 `/admin` 아래에만 있다.

### ★ 파생 결정 — 백엔드를 Next.js 뒤로 프록시한다

Next.js(Node)와 백엔드(자바)는 **다른 프로세스**다. 그러면 브라우저 입장에서 오리진이 둘이 되고,
**`httpOnly` 쿠키를 쓰는 우리 설계(`FR-013`)에 문제가 생긴다.**

**결정: Next.js의 rewrite로 `/api/*`를 백엔드로 넘긴다.** 브라우저에게는 **오리진이 하나**다.

| | 프록시 없이 (포트 둘) | **프록시 (채택)** |
|---|---|---|
| CORS 설정 | **필요** — `credentials` 허용 + 오리진 명시 | **불필요** |
| 쿠키 | 로컬에선 동작한다(쿠키는 포트를 구분하지 않는다). **배포에서 도메인이 갈리면 그때 깨진다** | 처음부터 같은 오리진 |
| 배포 시 | 오리진 구성을 다시 설계 | **그대로 간다** |

**"로컬에서는 되는데 배포에서 깨지는" 대표적 지점**이라 처음부터 프록시로 둔다.
`process.md` 6.2에서 호스팅을 미루기로 하며 *"로컬도 Docker로 띄워 이식성을 유지한다"* 고 한 것과
같은 성격의 판단이다.

**부수 이득**: `test-strategy.md` 4.1의 E2E `storageState`가 **오리진 하나만 다루면 되어 단순해진다.**

---

## 3. 티켓 식별 값 — 256비트 랜덤 문자열, DB에는 SHA-256 해시

**결정**: `SecureRandom` 256비트 → Base64url 문자열을 쿠키로. **DB에는 SHA-256 해시만** 저장.

**JWT를 쓰지 않는 근거**는 `data-model.md`에 이미 있었다 —
*"Refresh는 어차피 DB를 확인하므로 JWT일 필요가 없다."*
JWT로 만들면 **크기만 커지고 서명 검증 비용이 추가**되는데, 그 검증으로 얻는 것(DB 없이 판정)을
우리는 애초에 쓰지 않는다. 게다가 JWT 내용은 누구나 읽을 수 있어 담을 것도 없다.

### 왜 비밀번호는 bcrypt인데 티켓은 SHA-256인가

**뒤집힌 것처럼 보이므로 근거를 남긴다.**

| | 비밀번호 | 티켓 식별 값 |
|---|---|---|
| 값의 출처 | **사람이 만든다** → 추측 가능, 사전 공격 가능 | **256비트 난수** → 추측 불가 |
| 해시가 느려야 하나 | **✅ 느려야 한다** — 무차별 대입을 비싸게 만드는 것이 목적 | ❌ 느릴 이유가 없다. 대입할 공간이 없다 |
| 조회 방식 | `username`으로 행을 찾고 **그 행의 해시와 비교** | **해시로 행을 찾아야 한다** |

**결정적인 것은 마지막 줄이다. bcrypt는 솔트가 매번 달라서 같은 입력이 매번 다른 해시가 된다.**
그래서 `WHERE 해시 = ?` 로 **조회할 수가 없다.** 티켓은 해시로 찾아야 하므로 bcrypt를 쓸 수 없다.

SHA-256이면 같은 입력이 항상 같은 해시이므로 **유일 인덱스를 걸어 바로 찾을 수 있다.**
`data-model.md`가 티켓 식별 값에 유일 제약을 준 것과 맞아떨어진다.

---

## 4. 자격증명 수명 — 스펙 Assumptions 값을 확정

| | Access | Refresh |
|---|---|---|
| 사용자 | **30분** | **14일** |
| **관리자** | **15분** | **1일** |

스펙 Assumptions 1의 가정값을 그대로 확정한다. 실무 범위(Access 15분~1시간, Refresh 7~30일)
안에 있고, `AC-33`은 **"관리자가 더 짧다"는 관계만** 요구하므로 값이 바뀌어도 AC는 유지된다.

**환경변수로 둔다** — `data-model.md`가 *"수명은 데이터가 아니라 설정"* 이라고 판정했다.
사용자마다 다른 값이 아니라 **역할마다 정해진 값**이므로 행에 저장할 것이 없다.

---

## 5. 실패 횟수 저장 — 서버 메모리

**결정**: 서버 메모리(만료 기능이 있는 인메모리 캐시). **Redis를 도입하지 않는다.**

**근거**: `process.md` 6.2에서 호스팅을 미루고 로컬 우선으로 가면서 **서버가 1대로 확정**됐다.
`data-model.md`는 이미 *"실패 횟수는 사라지면 카운트가 0으로 돌아갈 뿐 → 영속 불필요"* 로 판정했다.
1대인데 Redis를 넣으면 **운영 대상이 하나 늘고 배울 것도 늘 뿐 얻는 것이 없다.**

**카운터는 둘이다** (`data-model.md` codex 권고 반영):

| 카운터 | 키 | 근거 |
|---|---|---|
| 인증 실패 횟수 | **계정 단위**(`username` 소문자 정규화) | `FR-027` 특정 계정을 노린 시도를 늦춘다 |
| 중복확인 호출 횟수 | **클라이언트 단위**(요청 출처 주소) | `FR-030` 계정이 특정되지 않은 열거를 막는다 |

**받아들인 대가**: **서버를 재시작하면 카운터가 0으로 돌아간다.** 배포할 때마다 방어가 초기화된다.
공격자가 배포 시점을 알 수 없고 재시작이 잦지 않으므로 감수한다.
**방아쇠: 서버가 2대 이상이 되는 시점**(각 서버의 메모리가 갈라져 방어가 무의미해진다).
호스팅 방아쇠(`process.md` 6.2)와 같은 시점이다.

---

## 6. 남용 방어 임계값

### 인증 실패 — 점진적 지연 (`FR-027`·`AC-29`)

| 누적 실패 | 응답 |
|---|---|
| 1~2회 | 지연 없음 |
| 3회 | 1초 지연 |
| 4회 | 2초 지연 |
| 5회 | 4초 지연 |
| **6회 이상** | **429 `AUTH_TOO_MANY_ATTEMPTS`**, 60초간 차단 |

- 카운터 윈도: 15분. **로그인 성공 시 즉시 초기화**
- **계정은 잠기지 않는다**(`FR-027` MUST NOT). 차단 시간이 지나면 올바른 비밀번호로 정상 로그인된다

### ★ 함정 — "지연"이 그 자체로 공격 도구가 된다

응답을 지연시키려고 **서버 스레드를 붙잡고 기다리면**, 공격자가 일부러 실패를 쌓아
**스레드를 고갈시킬 수 있다.** 방어 장치가 서비스 거부 수단이 되는 것이다.

**대응: 가상 스레드를 켠다**(`spring.threads.virtual.enabled=true`).
가상 스레드는 기다리는 동안 **OS 스레드를 점유하지 않는다.**

> **Java 25를 고른 것이 여기서 값을 한다.** 가상 스레드는 Java 21+ 기능이고
> `process.md` 6.1에서 25를 고른 이유는 지원 기간이었는데, **이 결정이 부수적으로
> 남용 방어의 함정을 막아준다.** 21로 갔어도 됐고 17이었으면 안 됐다.

### 중복확인 빈도 제한 (`FR-030`·`AC-30`)

- **분당 10회**, 초과 시 **429 `CHECK_ID_TOO_MANY_REQUESTS`**

**받아들인 대가**: 회사·학교처럼 **여러 사람이 출처 주소를 공유하는 환경에서는 카운터도 공유**된다.
남의 호출 때문에 막힐 수 있다. 그래서 임계값을 너무 낮추지 않았다.
근본 해결은 출처 주소 단위 방어를 인프라 계층으로 올리는 것이고, 스펙이 이미
**IP 단위 분산 공격을 범위 밖**으로 명시했다.

---

## 7. 만료된 티켓 행 청소

**결정**: 스프링 스케줄러로 **하루 1회**, 만료 시각이 지난 행을 삭제.

`data-model.md`가 넘긴 과제다 — *"DB에는 TTL이 없으므로 주기적으로 지우는 작업이 필요하다.
테이블을 만들면 청소 책임이 따라온다."*

**서버가 1대이므로 중복 실행 문제가 없다.** 여러 대가 되면 같은 작업이 동시에 여러 번 돌아
잠금 경합이 생긴다. → **방아쇠는 5번과 같다.**

---

## 8. 외래키 삭제 규칙

**결정**: 재발급 티켓의 유저 외래키에 **`ON DELETE CASCADE`**.

유저가 삭제되면 그의 티켓도 함께 삭제된다. **근거**: 티켓은 유저에 종속된 개념이고
**유저 없는 티켓은 의미가 없다.** 남겨두면 그 티켓으로 재발급을 시도할 때 유저를 찾지 못해
오류가 난다 — 고아 행이 장애 원인이 된다.

회원 탈퇴가 001 범위 밖이라 **당장은 발생하지 않지만 물리 설계이므로 지금 정한다**
(`data-model.md`가 4번으로 넘긴 항목).

---

## 9. `username` 대소문자 무시 구현 — `ADR-003`에서 이미 해소

`LOWER(username)`에 **함수 유니크 인덱스**를 만든다. `CITEXT` 확장은 쓰지 않는다.
**금지 아이디 목록도 같은 규칙을 적용한다**(`data-model.md` 확정 — `admin`이 금지면
`Admin`·`ADMIN`도 거부).

**`ADR-003` 리스크 1을 여기서 받는다**: `ddl-auto=validate`는 **함수 인덱스 일치를 검증하지 않는다.**
엔티티에 `unique = true`만 걸면 실제 인덱스와 어긋날 수 있다.
→ **`test-strategy.md` 5.2의 동시 가입 경합 테스트에 대소문자가 다른 아이디(`Kim` vs `kim`)
경합을 반드시 포함한다.** 그것이 이 인덱스를 확인하는 유일한 장치다.

---

## 10. JWT 라이브러리 — Spring Security 내장(Nimbus)

Spring Security가 JWT 발급·검증을 내장 지원하므로 **별도 의존성을 추가하지 않는다.**
`process.md` 누적 항목 12번이 *"ADR 대상 아님(표준이라 교체 비용이 낮음)"* 으로 판정한 항목이다.

**Access 토큰만 JWT다.** Refresh는 3번에 따라 랜덤 문자열이다.

---

## 11. 탐색 상태 (`FR-020`·`AC-24`) — 쿼리 파라미터 + 목적지 검증

**결정**: 로그인 화면으로 보낼 때 `?redirect=<경로>`로 넘기고,
**로그인 후 이동 직전에 목적지를 검증**한다. 같은 출처의 **상대 경로만** 허용한다.

`data-model.md`가 *"DB가 아니다. 수명은 수십 초, 사라지면 홈으로 갈 뿐 무해"* 로 판정했다.

**검증이 없으면 오픈 리다이렉트 취약점이 된다** — `?redirect=https://공격자.com` 이 통하면
우리 로그인 화면을 거쳐 공격자 사이트로 보내는 피싱 링크를 만들 수 있다.

**스펙에 이것을 막는 AC가 없었다** → `AC-34`를 추가한다(아래 12번).

**검증 위치가 화면인 것이 `FR-002`·`FR-019`의 예외인가 — 아니다.**
그 두 요구사항은 **입력 검증**과 **권한 검증**을 서버에서 하라는 것이다.
목적지 이동은 **브라우저가 수행하는 행위**이므로 그것을 막는 자리도 브라우저 쪽이다.
서버는 이동에 관여하지 않으므로 서버에서 막을 대상 자체가 없다.

---

## 12. 스펙에 되돌린 것 — AC 3건

계획을 쓰다 발견한 스펙 문제다. `process.md` 게이트 규칙의 *"설계 문제 → 스펙/plan 수정"* 경로다.

### ① `AC-34` 신규 — 오픈 리다이렉트 차단

11번의 취약점을 막는 AC가 없었다.
**3.9에서 정한 규칙을 그대로 적용했다** — *"AC로 명시되지 않은 코드 경로가 중요하다면
AC가 빠진 것이므로 스펙 결함으로 다룬다. 테스트 전략이 스펙의 빈틈을 메우려 하지 않는다."*

→ **커버리지 게이트를 AC 100%로 정한 판단이 실제로 결함을 하나 잡았다.**

### ② `AC-25`를 006에서 검증하도록 이관

```
AC-25  When 문의게시판 목록 조회를 요청하면  Then 200이고 목록이 반환된다
```

**문의게시판은 006 스토리다.** 001에 그 리소스가 없다.

스텁(빈 목록을 반환하는 껍데기)을 만드는 것을 **기각**했다 — 실체가 없는
**테스트를 위한 코드**이고, 006에서 실제로 만들 때 버려진다.

→ 스펙에 **"이 AC는 006에서 검증"** 을 명시하고 커버리지 계산도 006으로 옮긴다.
**커버리지 게이트에 규칙이 하나 필요해진다** — 없으면 *"미룬 AC"* 와 *"잊은 AC"* 가 구분되지 않는다.
→ `test-strategy.md` 5.3에 추가한다.

### ③ `AC-21`~`23`·`AC-26`의 "관리자 전용 기능"에 실체를 준다

이것도 001에 없었지만 **스텁이 아닌 답이 있다.**

| 엔드포인트 | 용도 | 나중에 버려지나 |
|---|---|---|
| `GET /api/me` | 인증 필요. **`AC-4` 자동 재발급 검증에도 필요** | ❌ 화면 헤더의 로그인 정보로 계속 쓴다 |
| `GET /api/admin/me` | `ADMIN` 전용. `AC-21`·`22`·`23`·`26`의 대상 | ❌ 관리자 화면 헤더로 계속 쓴다 |

**실제로 쓸모가 있으므로 스텁이 아니다.**

---

## 결정 요약

| # | 항목 | 결정 | ADR |
|---|---|---|---|
| 1 | ORM | Spring Data JPA. 보조 조회 도구는 002까지 미룸 | **`ADR-004`** |
| 2 | 프론트 | Next.js 하나 + **백엔드를 rewrite로 프록시** | — |
| 3 | 티켓 식별 값 | 256비트 랜덤 + SHA-256 해시 저장 | — |
| 4 | 자격증명 수명 | 사용자 30분/14일, 관리자 15분/1일 (환경변수) | — |
| 5 | 실패 횟수 | 서버 메모리. 카운터 2개 | — |
| 6 | 남용 임계값 | 3회부터 지연, 6회 429. 중복확인 분당 10회. **가상 스레드 필수** | — |
| 7 | 티켓 청소 | 스케줄러 하루 1회 | — |
| 8 | FK 삭제 | `ON DELETE CASCADE` | — |
| 9 | 대소문자 무시 | `LOWER()` 함수 유니크 인덱스 | `ADR-003` |
| 10 | JWT 라이브러리 | Spring Security 내장 | — |
| 11 | 탐색 상태 | 쿼리 파라미터 + 목적지 검증 | — |
| 12 | 스펙 수정 | `AC-34` 추가 · `AC-25` 006 이관 · 관리자 엔드포인트 실체화 | — |

**NEEDS CLARIFICATION 0건.**
