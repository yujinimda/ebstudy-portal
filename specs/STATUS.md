# 현재 상태 — 세션 인수 문서

> **새 세션을 시작할 때 이 문서를 먼저 읽는다.**
> 프로세스 규칙은 `process.md`, 여기는 **지금 어디까지 왔고 다음에 무엇을 하는가**만 담는다.
> 작업이 진행되면 이 문서를 갱신한다.

**마지막 갱신**: 2026-07-29 (4번 스택 확정)

---

## 프로젝트 한 줄

게시판 포털(게시판 4종 × 사용자/관리자)을 만들면서, **자바·백엔드를 처음 배우는 프론트엔드
개발자가 기술 선택의 근거와 트레이드오프를 산출물로 남기는 것**이 코드 완성과 동등한 목표다.

- 레포: https://github.com/yujinimda/ebstudy-portal (public)
- 요구사항 원천은 제3자 소유 비공개 문서. **레포에 문서도 출처도 남기지 않는다** (`process.md` 8장)

---

## 현재 위치 — 001 인증

```
0    레포·CI·constitution        ✅  PR #1~#3
1    브레인스토밍                 ✅  ADR-001 (인증 = JWT Access+Refresh)
2    스펙 작성                   ✅  spec.md — AC 33개 · FR 34개
3    스펙 검증 (clarify)         ✅  미확정 0건
3.5  DB 논리 설계                ✅  테이블 3개 · ADR-002 (PK 전략)
3.9  테스트 전략                 ✅  미결 0건 · constitution 1.3.0
4    기술 계획 (스택·호스팅)      🔄  ← 여기
5    codex 계획 검증              ⬜
6    작업 분해 → 6.7 마이그레이션 → 7 구현
```

**머지된 PR**: #1~#9. **main 직접 push 0회.**

---

## 4번에서 확정한 스택 (2026-07-29)

| 항목 | 결정 | 왜 |
|---|---|---|
| 자바 | **Java 25 LTS** | Java 21의 Oracle 무료 지원이 **2026-09 만료**(2개월 뒤). 25는 2033까지 |
| 프레임워크 | **Spring Boot 4.1** | **3.x 라인 전체 EOL**(3.5가 2026-06-30). 실서비스에 EOL은 CVE 패치를 못 받는다 |
| 빌드도구 | **Gradle (Kotlin DSL)** | 스프링 최신 예제가 Gradle 우선 + TDD로 테스트를 자주 돌리는 흐름에 증분 빌드가 맞다 |
| **DBMS** | **PostgreSQL** → `ADR-003` | **DDL이 트랜잭션 안에서 돈다** → 손으로 쓴 마이그레이션이 실패하면 반쪽 적용이 안 남는다. `username` 대소문자 무시를 `LOWER()` 인덱스로 **스키마에 보이게** |
| **9.5 QA 환경** | **로컬 (Docker Compose)** | 무료로 PR별 프리뷰를 주는 곳이 없고, 1인 개발이라 로컬 체크아웃이 동등하다 |
| 호스팅 | **미룸** — 방아쇠 있음 | `process.md` 6.2. **방아쇠: 001 구현이 끝나 남에게 보여줄 필요가 생기는 시점** |

**대가 (알고 받았다)**: 인터넷 예제 대다수가 아직 Spring Boot **3.x**다. 검색한 예제가
그대로 안 돌아가는 일을 겪는다. **불편이지 위험이 아니고, 반대(EOL)는 위험이다.**

**이번에 사용자가 잡은 것**: Claude가 유료 후보 4개만 내놓자 *"공짜인거 없어? 아니면 로컬"* 을
물었다. **무료·로컬이 목록에서 빠져 있었다**(Claude 과실). 재분석하니 호스팅이 물고 있다던
3가지 중 **실제로 물린 것은 9.5뿐**이어서 결론이 "$5 지불"에서 "**지금 정하지 않는다**"로
뒤집혔다. → `process.md` 7.3에 **AI의 목록을 사람이 감시한 첫 사례**로 기록.

또 **DBMS가 애초에 미결정 목록에 없었다** — `ADR-002`의 UUID 누락과 같은 종류의 Claude 과실.

---

## 3.9에서 확정한 것 (2026-07-29)

| | 결정 |
|---|---|
| 커버리지 | **AC 커버리지 100%가 게이트, 줄 커버리지는 게이트가 아님**(측정·리포트만) |
| E2E 도구 | **Playwright** |
| 파생 | `test-strategy.md` **4.1 신설** — 로그인 비용을 없애는 3단계 실행 방법 |
| 파생 | **constitution 1.3.0** — 원칙 II에 커버리지 게이트 추가 |
| 파생 | **plan-template Constitution Check 항목 II 보강** — 형식만 대조하고 "모든 AC가 테스트를 갖는가"는 안 걸리던 빈틈 |

4.1이 생긴 계기: 사용자가 도구 확정 시점에 *"로그인에 많이 막히는데 빠르게 하는 법 있어?"* 를
물어, 규칙 2가 **"저장하고 주입"이라는 한 줄로만 적혀 실행 방법이 없던 것**이 드러났다.
`process.md` 7.3에 **같은 영역에서 두 번 주도한 첫 사례**로 기록했다.

---

## 바로 다음 할 일

### 4번 기술 계획 — `/speckit-plan` 실행

스택이 확정됐으므로 이제 `plan.md`를 쓸 수 있다. **호스팅은 미뤘고 방아쇠를 박았으므로
더 이상 막고 있지 않다.**

계획에서 001 범위로 확정해야 할 것 (누적 표 참조):

| | 항목 | 비고 |
|---|---|---|
| 4 | 자격증명 수명 | 가정: 사용자 30분/14일, 관리자 15분/1일 |
| 6 | 티켓 식별 값의 형태 | Refresh는 어차피 DB를 보므로 JWT일 필요가 없다 |
| 7 | 실패 횟수 저장 위치 | **로컬 우선이라 서버 1대 → 메모리로 시작 가능해졌다** |
| 8 | 남용 방어 임계값 | 계정 단위 / 클라이언트 단위 **분리** |
| 10 | 만료된 티켓 행 청소 | DB에 TTL이 없으므로 주기 작업 |
| — | ORM | JPA vs MyBatis. **`process.md` 4.1이 ADR 대상으로 명시한 항목** |
| — | 프론트 구성 | 001에 필요한 만큼만. 사용자 SPA + 관리자 MPA 분리는 004 |

게이트: **Constitution Check 통과**(위반 0건 또는 ADR로 근거 기록) → 5번 codex 검증.
**`ADR-003`으로 항목 5(`username` 대소문자 무시 구현)는 이미 해소됐다.**

---

## ★ 사람이 채워야 할 빈칸 8개

| 파일 | 칸 | 소재 |
|---|---|---|
| `decisions/ADR-001-인증-방식.md` | `내 판단` | 이미 답했음 — "회사에서 JWT가 기본" |
| | `몰랐던 지점` | "무상태면 탈취에 바로 대응할 수 있다고 반대로 알고 있었다" |
| `decisions/ADR-002-전역-기본키-전략.md` | `내 판단` | 문자열 복제 방지 · JPA 정합 |
| | `몰랐던 지점` | 기본키가 뭔지 자체를 몰랐음 / 로그인 아이디와 DB `id`가 다른 것 / JWT가 암호화가 아니라 서명이라는 것 |
| `decisions/ADR-003-DBMS-선택.md` | `내 판단` | **추천을 그대로 골랐다 — 그렇게 써도 된다.** 근거 중 무엇이 가장 납득됐는지(마이그레이션 실패 시 되돌아간다 / 규칙이 스키마에 보인다)를 쓰면 충분 |
| | `몰랐던 지점` | DDL이 트랜잭션 안에서 돈다/안 돈다는 구분 / 콜레이션이 뭔지 / DB를 고르지 않으면 마이그레이션 SQL을 못 쓴다는 것 |
| `learning/2026-07-29-dbms.md` | `내 말로 다시` | 데이터베이스와 DBMS의 차이 / 파일 대신 DBMS를 쓰는 이유 하나. **PostgreSQL·MySQL·Oracle 비교 절도 포함** |
| | `아직 모르는 것` | 이 설명을 읽고도 흐릿한 것 |

**이 칸이 이 프로젝트의 목적이다.** 코드는 AI가 쓸 수 있지만 이 칸은 못 쓴다.
`learning/2026-07-27-stateless-jwt-session.md`의 `내 말로 다시`는 채워져 있고,
결론이 뒤집혔던 것을 정정 블록으로 보존했다 — **그 간극 자체가 값진 기록**이라 원문을 지우지 않았다.

---

## 4번 기술 계획에서 반드시 결정할 것 (누적)

| # | 항목 | 상태 |
|---|---|---|
| 1 | **백엔드 호스팅** | ✅ **미룸 확정** — `process.md` 6.2에 방아쇠. 더 이상 아무것도 막지 않는다 |
| 2 | 스택 상세 | ✅ Java 25 · Spring Boot 4.1 · Gradle · PostgreSQL. **ORM(JPA vs MyBatis)과 프론트 구성만 남았다** |
| 3 | 첨부파일 저장 위치 | ⬜ **002로 미룸** — 1번과 묶여 있고 001 범위가 아니다 |
| 4 | 자격증명 수명 구체값 | ⬜ **plan에서** 가정: 사용자 30분/14일, 관리자 15분/1일 |
| 5 | `username` 대소문자 무시 **구현 방법** | ✅ **`ADR-003`으로 해소** — `LOWER(username)` 함수 유니크 인덱스. `CITEXT` 확장은 쓰지 않는다 |
| 6 | 티켓 식별 값의 형태 | ⬜ **plan에서.** Refresh는 어차피 DB를 보므로 JWT일 필요가 없다 |
| 7 | 실패 횟수 저장 위치 | ⬜ **plan에서.** 로컬 우선이라 **서버 1대가 확정** → 메모리로 시작 가능. Redis를 지금 넣을 이유가 없어졌다 |
| 8 | 남용 방어 임계값 | ⬜ **plan에서.** 카운터는 **계정 단위/클라이언트 단위 둘로 분리** |
| 9 | IP 단위 분산 공격 방어 | ⬜ 인프라 계층(리버스 프록시·WAF) 몫 → **호스팅 방아쇠와 함께** |
| 10 | 만료된 티켓 행 청소 | ⬜ **plan에서.** DB에 TTL이 없으므로 주기 작업 필요 |
| 11 | 유저 삭제 시 티켓 처리 | ⬜ FK 삭제 규칙. 회원 탈퇴가 001 범위 밖이라 당장 발생하지 않음 |
| 12 | JWT 라이브러리 | ⬜ ADR 대상 아님(표준이라 교체 비용이 낮음) |
| 13 | Testcontainers 구성 | ⬜ **이제 가능** — Postgres 확정. 이미지 태그를 로컬 Compose와 **같은 값으로 고정**한다(`ADR-003` 리스크 2) |
| 14 | E2E 실행 환경 | ⬜ 4.1의 setup이 붙을 곳. **상태 파일(`auth/*.json`)을 `.gitignore`에 추가**해야 한다 — 자격증명이다 |

`.env.example`은 **채웠다** — `DB_URL`/`DB_USERNAME`/`DB_PASSWORD`, `JWT_SECRET`,
`ADMIN_INITIAL_ID`/`ADMIN_INITIAL_PASSWORD`. 수명·임계값 키는 plan에서 확정 후 추가한다.

---

## 읽는 순서 (새 세션)

| # | 파일 | 왜 |
|---|---|---|
| 1 | `specs/STATUS.md` | 이 문서 |
| 2 | `specs/process.md` | **프로세스 단일 진실.** 특히 5.5(작업 리듬·판단 위임 — **0번 용어 확인 신설**)와 7장(결정 이력 39건) |
| 3 | `.specify/memory/constitution.md` | 원칙 8개. `/speckit-plan`이 매번 대조한다. **v1.3.0** |
| 4 | `specs/001-user-auth/spec.md` | 만들 것의 정의. AC 33개 |
| 5 | `specs/data-model.md` | 테이블 3개 + 판단 도구 3개. **물리 설계는 `ADR-003` 파생 규칙** |
| 6 | `specs/test-strategy.md` | 3.9. **미결 0건.** 4.1(로그인 비용 제거)과 5장(결정 4건) |
| 7 | `specs/decisions/` | ADR-001(인증) · ADR-002(PK) · **ADR-003(DBMS)** |
| 8 | `specs/learning/` | 무상태·서명 개념 · **DBMS** |

---

## 협업 방식 (반드시 유지)

`process.md` 5.5에 규칙으로 있고, 실제로 지켜야 하는 것:

1. **판단 근거가 없는 영역에서 3지선다를 던지고 "당신 근거로 답하라"고 하면 작동하지 않는다.**
   → ① 실무가 어떻게 하는지 먼저 알려준다 ② Claude가 정하고 이유를 설명한다
   ③ "이 이유가 납득되나요, 걸리는 데가 있나요?"로 **검증을 요청한다**
2. **실재하는 대안을 빠뜨리면 그 자체가 위반이다** (constitution 1.1.0).
   사람은 제시된 목록 밖의 선택지를 볼 방법이 없다.
3. **작업 단위마다 커밋·PR·머지까지 끝낸 뒤 멈추고 사람이 읽을 시간을 준다.**
4. **재검증하지 않는다.** 1차 검증 지적을 반영하면 통과. 대신 **무엇을 어떻게 고쳤는지 기록**한다.
5. 개념은 **비유로 설명하면 잘 통했다** — 세션=클럽 팔찌, JWT=위조 불가 도장 찍힌 신분증,
   Refresh 저장=명단(장부).
6. 설명 후 **"아직 흐릿한 데 있나요?"** 로 확인한다. 실제로 여러 번 오해가 잡혔다.

---

## 운영 함정 (겪은 것)

| | 증상 | 대응 |
|---|---|---|
| **gh 계정 전환** | push 직전 활성 계정이 회사 계정(`yujinebrain`)으로 되돌아가 403 | **push 전 `gh auth status` 확인**, 필요시 `gh auth switch --user yujinimda` |
| **codex exec 무응답** | 16분간 출력 없이 멈춤. `Reading additional input from stdin...` | **`< /dev/null` 로 stdin을 닫는다.** 필수 |
| codex 출력 파이프 | `\| tail`로 파이프하면 끝날 때까지 진행이 안 보인다 | 파이프하지 않는다 |
| 한글 깨짐 | 질문 UI에 한글이 `생답`·`토굴` 등으로 깨짐 | 유니코드 이스케이프(`\uXXXX`)로 인코딩하지 말고 **한글을 그대로** 쓴다 |
